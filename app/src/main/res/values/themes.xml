<resources xmlns:tools="http://schemas.android.com/tools">
  <!-- Base application theme. -->
  <style name="Base.Theme.MiniProyecto1" parent="Theme.Material3.DayNight.NoActionBar">
    <!-- Customize your light theme here. -->
    <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
  </style>

  <style name="Theme.MiniProyecto1" parent="Base.Theme.MiniProyecto1" />
  <style name="toolbarTheme" parent="ThemeOverlay.AppCompat.ActionBar">
    <item name="android:colorControlNormal">@color/white</item>
    <item name="titleTextColor">@color/white</item>

  </style>

  <style name="Theme.InventoryWidget.AppWidgetContainer" parent="Theme.Material3.DayNight.NoActionBar">
    <item name="android:background">@color/black_specific</item>
  </style>
</resources>